{
  "name": "12-test-gig",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "backend/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "deploy:saas": "npm run build --workspace=saas-panel && npx firebase-tools deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "lucide-react": "^1.33.0",
    "react-router-dom": "^7.18.2"
  }
}
