# Setup Guide

1. Clone repository.
2. Ensure Node.js 18+ is installed.
3. Run `npm install` in the root.
4. Copy `apps/web/.env.example` to `apps/web/.env.local` and add Firebase config.
5. Run `npm run dev` to start the Next.js SaaS panel.
6. Build shared packages: `npm run build` in root.
